import React from 'react'
import {StyleSheet, View, Text} from 'react-native'

export const AddBetScreen = props => {
    return (
        <View>
            <Text>Add Bet Screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})